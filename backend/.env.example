# Authentication & Security
SECRET_KEY=your-super-secret-key-change-this-in-production-min-32-chars-long
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Database Configuration (SQLAlchemy for auth)
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5433/trading_hub

# Frontend Configuration
FRONTEND_URL=http://localhost:5173

# Database Configuration (asyncpg for orderbook data)
DB_HOST=127.0.0.1
DB_PORT=5433
DB_NAME=trading_hub
DB_USER=postgres
DB_PASSWORD=postgres

# Database Pool Settings (optional)
DB_POOL_MIN_SIZE=2
DB_POOL_MAX_SIZE=10
DB_POOL_TIMEOUT=30
DB_COMMAND_TIMEOUT=60
DB_MAX_RETRIES=3
DB_RETRY_DELAY=1.0

# C++ Analytics Engine
USE_CPP_ENGINE=true
CPP_ENGINE_HOST=localhost
CPP_ENGINE_PORT=50051
